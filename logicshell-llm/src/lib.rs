// logicshell-llm: LLM bridge, context, composer, Ollama client
// Enable HTTP backend with feature `ollama`

#[cfg(test)]
mod tests {
    /// Phase 1 smoke: LLM crate builds — NFR-09, NFR-10
    #[test]
    fn workspace_compiles() {}
}
